package move;

import java.awt.*;

/*
public class MovementValidator {

    public boolean isIdle(int dx, int dy) {

        return dx == 0 && dy == 0;
    }

    public Point computeNewHead(
            Point head,
            int dx,
            int dy
    ) {

        Point newHead = new Point(head);

        newHead.x += dx;
        newHead.y += dy;

        return newHead;
    }
}
*/